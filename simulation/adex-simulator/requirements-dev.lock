# Regenerate deliberately after reviewing release notes and advisories.
# Direct and transitive dependencies are exact so clean CI installs resolve
# the same environment. The project itself has no runtime dependencies.
colorama==0.4.6 ; sys_platform == "win32"
iniconfig==2.3.0
packaging==26.2
pluggy==1.6.0
Pygments==2.20.0
pytest==9.1.1
ruff==0.15.22
