packages:
  - "packages/*"
  - "apps/*"

# esbuild ships a prebuilt platform binary through a postinstall script; Vite and
# Vitest cannot run without it. Every other dependency stays script-free.
allowBuilds:
  esbuild: true
